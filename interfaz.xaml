<Window x:Class="ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Optimización" Height="300" Width="400" Background="Black" WindowStyle="None">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock x:Name="MessageLabel" Text="Optimizando Windows, Espere..." FontSize="14" Foreground="White" HorizontalAlignment="Center" />
        <TextBlock x:Name="PercentageLabel" Text="0% Completado" FontSize="14" Foreground="White" HorizontalAlignment="Center" />
        <TextBlock x:Name="InfoLabel" Text="No apagues el equipo" FontSize="14" Foreground="White" HorizontalAlignment="Center" />
        <TextBlock x:Name="SupportLabel" Text="Soporte y Aportes" FontSize="14" Foreground="White" HorizontalAlignment="Center" />
        <ProgressBar x:Name="ProgressBar" Height="20" Width="300" Foreground="Green" Background="DarkGray" />
    </StackPanel>
</Window>

